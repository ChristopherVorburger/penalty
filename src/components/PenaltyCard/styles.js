import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  penalty_card: {
    color: "#b2102f",
  },
  penalty_card__done: {
    backgroundColor: "#eeeeee!important",
  },
  penalty_card__checked: {
    color: "green",
  },
  penalty__icons: {},
}));
