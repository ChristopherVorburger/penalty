import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  add_penalty__button_submit: {
    transition: "all .2s ease-in-out!important",
    "&:hover": {
      transition: "all .2s ease-in-out",
      transform: "scale(1.02)",
    },
  },
}));
